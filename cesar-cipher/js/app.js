//Validación
function cipher (text) {
var text = prompt("Escribe una frase"); {
  for (i=0; i <=text.length; i++) {
    if (typeof test[i] == "number") {
      return Invalid
    } //else {
      //Mandar al cifrado
    }
  }
}
}
